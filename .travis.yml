language: c
compiler:
  - gcc
sudo: required
services:
  - docker

matrix:
    include:
        - env: "job1"
          script:
            - ./configure
            - make -C ci-tests job1
        - env: "job2"
          script:
            - ./configure
            - travis_wait make -C ci-tests job2

